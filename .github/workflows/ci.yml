name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: reservation_terrain
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build and Test Backend
      working-directory: ./Desktop/gestion-de-reservation-d-un-terrain-de-proximite-main/gestion-de-reservation-d-un-terrain-de-proximite-main/backend
      env:
        SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/reservation_terrain
        SPRING_DATASOURCE_USERNAME: user
        SPRING_DATASOURCE_PASSWORD: password
        SPRING_JPA_HIBERNATE_DDL_AUTO: update
      run: |
        chmod +x mvnw
        ./mvnw clean install

  frontend-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18
        cache: 'npm'
        cache-dependency-path: ./Desktop/gestion-de-reservation-d-un-terrain-de-proximite-main/gestion-de-reservation-d-un-terrain-de-proximite-main/frontend/package-lock.json

    - name: Install Frontend Dependencies
      working-directory: ./Desktop/gestion-de-reservation-d-un-terrain-de-proximite-main/gestion-de-reservation-d-un-terrain-de-proximite-main/frontend
      run: npm install

    - name: Run Frontend Tests
      working-directory: ./Desktop/gestion-de-reservation-d-un-terrain-de-proximite-main/gestion-de-reservation-d-un-terrain-de-proximite-main/frontend
      run: npx ng test --watch=false --browsers=ChromeHeadless

    - name: Build Frontend
      working-directory: ./Desktop/gestion-de-reservation-d-un-terrain-de-proximite-main/gestion-de-reservation-d-un-terrain-de-proximite-main/frontend
      run: npm run build
